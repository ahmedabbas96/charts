apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: go-app-gateway
  namespace: default  
spec:
  selector:
    istio: gateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"   # or "*" for testing