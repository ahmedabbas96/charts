apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: github-egress
  namespace: istio-system
spec:
  hosts:
    - github.com
    - api.github.com
    - ghcr.io
  location: MESH_EXTERNAL
  resolution: DNS
  ports:
    - number: 443
      name: https
      protocol: TLS